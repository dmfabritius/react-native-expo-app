/* eslint-disable no-undef */
// jest.mock('react-native/Libraries/Animated/NativeAnimatedHelper');
// jest.mock('react-native/Libraries/Animated/src/NativeAnimatedHelper');
// jest.mock('./src/providers/StoreProvider'); // implementation in __mocks__ subfolder

// jest.spyOn(Auth, 'currentAuthenticatedUser').mockImplementation(() => Promise.resolve({ refreshSession: jest.fn() }));
// jest.spyOn(Auth, 'signOut').mockImplementation(() => Promise.resolve());
// jest.spyOn(Auth, 'currentUserInfo').mockImplementation(() =>
//   Promise.resolve({
//     attributes: {
//       'custom:roles': '[{"id":230, "customerId":1, "role":"OWNER"}]',
//       'custom:userId': '186',
//     },
//   }),
// );

// console.error = jest.fn();
// console.warn = jest.fn();
